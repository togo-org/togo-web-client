const useStyles = () => {
  return {
    root: {
      position: 'absolute',
      top: '33%',
      left: '50%',
      transform: 'translate(-50%, -50%)',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
    },
  };
};

export default useStyles;
