const useStyles = () => {
  return {
    root: {
      display: 'flex',
      height: '100%',
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'center',
    },
  };
};

export default useStyles;
