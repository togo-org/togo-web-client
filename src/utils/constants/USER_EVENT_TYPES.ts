import { UserEventType } from '@/types';

const USER_EVENT_TYPES = {
  RESTAURANT: 'restaurant' as UserEventType,
  TRIP: 'trip' as UserEventType,
  ATTRACTION: 'attraction' as UserEventType,
};

export default USER_EVENT_TYPES;
