const useStyles = () => {
  return {
    root: {
      position: 'absolute',
      top: '108px',
      left: '50%',
      transform: 'translate(-50%, -50%)',
      display: 'flex',
      flexDirection: 'row',
      width: '100%',
      alignItems: 'center',
      justifyContent: 'center',
    },
  };
};

export default useStyles;
