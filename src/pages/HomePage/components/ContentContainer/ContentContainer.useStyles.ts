const useStyles = () => {
  return {
    root: {
      position: 'absolute',
      border: '1px solid red',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      width: '100%',
      height: '100%',
    },
  };
};

export default useStyles;
