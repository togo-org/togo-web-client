const useStyles = () => {
  return {
    root: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'flex-start',
      justifyContent: 'flex-start',
    },

    label: {
      fontFamily: 'Rubik',
      fontWeight: 400,
      fontSize: '12px',
    },

    value: {
      fontFamily: 'Rubik',
      fontWeight: 500,
      fontSize: '1rem',
    },
  };
};

export default useStyles;
