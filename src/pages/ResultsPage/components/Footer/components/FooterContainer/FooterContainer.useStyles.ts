const useStyles = () => {
  return {
    root: {
      justifyContent: 'space-evenly',
      alignItems: 'center',
      flexDirection: 'row',
      display: 'flex',
      width: '100%',
    },
  };
};

export default useStyles;
