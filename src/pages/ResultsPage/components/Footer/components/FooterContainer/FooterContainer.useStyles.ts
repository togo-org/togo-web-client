const useStyles = () => {
  return {
    root: {
      justifyContent: 'space-evenly',
      alignItems: 'center',
      flexDirection: 'row',
      display: 'flex',
    },
  };
};

export default useStyles;
